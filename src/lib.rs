
/// A simple library that allows the user to input a string and parse it to a Function. Then the
/// functions the be derived and integrated (numerical definite integral). It also allows the user
/// to define custom functions and symbols and use them while building new functions with the help
/// of the Context object
pub mod functions;
